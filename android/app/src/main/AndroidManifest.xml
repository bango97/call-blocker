<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for CallScreeningService on API 24-28 -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />

    <application
        android:name=".CallBlockerApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.CallBlocker"
        android:allowBackup="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            CallScreeningService — intercepts incoming calls for pattern matching.
            On API 29+: system grants ROLE_CALL_SCREENING (covers contacts too).
            On API 24-28: app must be set as the default dialer.
        -->
        <service
            android:name=".service.BlockingCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

    </application>

</manifest>
